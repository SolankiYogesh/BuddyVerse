import {ms, ScaledSheet} from 'react-native-size-matters';
import {colorPalates} from 'theme';

export default ScaledSheet.create({
  container: {},
  buttonTitle: {
    fontSize: ms(16),
    color: colorPalates.blackShade1B,
  },
});
